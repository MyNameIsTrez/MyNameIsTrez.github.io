class Firework {
  constructor(x) {
    this.x = x;
    this.y = height;
    this.vel = vel;
    this.acc = acc;
  }

  
  this.acc = this.force

move() {
  TouchLis
  this.y += this.v;
}

draw() {
  ellipse(this.x, this.y, 30);
}
}